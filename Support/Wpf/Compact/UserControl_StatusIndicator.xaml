<UserControl x:Class="Support.Wpf.UserControl_StatusIndicator"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Support.Wpf"
             mc:Ignorable="d" d:Width="100" d:Height="100">
    <UserControl.Resources>
        <local:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
        <local:StatusToVisibilityConverter x:Key="StatusProcessingConverter" TargetStatus="Processing"/>
        <local:StatusToVisibilityConverter x:Key="StatusStopConverter" TargetStatus="Stop"/>
        <local:StatusToVisibilityConverter x:Key="StatusErrorConverter" TargetStatus="Error"/>
        <local:StatusToVisibilityConverter x:Key="StatusPausedConverter" TargetStatus="Paused"/>
        <local:StatusToVisibilityConverter x:Key="StatusCompletedConverter" TargetStatus="Completed"/>

    </UserControl.Resources>
    <Viewbox>
        <Canvas Height="200" Width="200">
            <Path Visibility="{Binding RelativeSource={RelativeSource AncestorType={x:Type local:UserControl_StatusIndicator}},Path=Status,Converter={StaticResource StatusProcessingConverter},NotifyOnSourceUpdated=True, UpdateSourceTrigger=PropertyChanged}"
                Fill="#FFD9DADE" Data="M 0 100 a 100,100 0 1 1 200,0 a 100,100 0 1 1 -200,0 M 30 100 a 70,70 0 1 1 140,0 a 70,70 0 1 1 -140,0"/>
            <Path Visibility="{Binding RelativeSource={RelativeSource AncestorType={x:Type local:UserControl_StatusIndicator}},Path=Status,Converter={StaticResource StatusProcessingConverter},NotifyOnSourceUpdated=True, UpdateSourceTrigger=PropertyChanged}"
                Fill="{Binding RelativeSource={RelativeSource AncestorType={x:Type local:UserControl_StatusIndicator}},Path=SpinnerColor,NotifyOnSourceUpdated=True, UpdateSourceTrigger=PropertyChanged}" Data="M 0 100 a 100,100 0 0 1 100,-100 v 30 a 70,70 0 0 0 -70,70" RenderTransformOrigin="1,1">
                <Path.RenderTransform>
                    <TransformGroup>
                        <ScaleTransform/>
                        <SkewTransform/>
                        <RotateTransform Angle="-3"/>
                        <TranslateTransform/>
                    </TransformGroup>
                </Path.RenderTransform>
                <Path.Triggers>
                    <EventTrigger RoutedEvent="Loaded">
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation Storyboard.TargetProperty="(Path.RenderTransform).(TransformGroup.Children)[2].(RotateTransform.Angle)" From="0" To="360" Duration="{Binding RelativeSource={RelativeSource AncestorType={x:Type local:UserControl_StatusIndicator}},Path=Duration,NotifyOnSourceUpdated=True, UpdateSourceTrigger=PropertyChanged}" RepeatBehavior="Forever"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </EventTrigger>
                </Path.Triggers>
            </Path>
            <TextBlock Text="⏹️" FontSize="150" Foreground="Blue"
                Visibility="{Binding RelativeSource={RelativeSource AncestorType={x:Type local:UserControl_StatusIndicator}},Path=Status,Converter={StaticResource StatusStopConverter},NotifyOnSourceUpdated=True, UpdateSourceTrigger=PropertyChanged}"/>
            
            <TextBlock Text="✔️" FontSize="150" Foreground="Green" 
                Visibility="{Binding RelativeSource={RelativeSource AncestorType={x:Type local:UserControl_StatusIndicator}},Path=Status,Converter={StaticResource StatusCompletedConverter},NotifyOnSourceUpdated=True, UpdateSourceTrigger=PropertyChanged}"/>

            <TextBlock Text="❌" FontSize="150" Foreground="Red" 
              Visibility="{Binding RelativeSource={RelativeSource AncestorType={x:Type local:UserControl_StatusIndicator}},Path=Status,Converter={StaticResource StatusErrorConverter},NotifyOnSourceUpdated=True, UpdateSourceTrigger=PropertyChanged}"/>

            <TextBlock Text="⏸️" FontSize="150" Foreground="Yellow"
             Visibility="{Binding RelativeSource={RelativeSource AncestorType={x:Type local:UserControl_StatusIndicator}},Path=Status,Converter={StaticResource StatusPausedConverter},NotifyOnSourceUpdated=True, UpdateSourceTrigger=PropertyChanged}"/>
        </Canvas>
    </Viewbox>
</UserControl>
