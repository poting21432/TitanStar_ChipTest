<UserControl x:Class="Support.Wpf.WaveDisplay.UserControl_WaveDisplay"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Support.Wpf.WaveDisplay"
             mc:Ignorable="d" Loaded="UserControl_Loaded"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Canvas x:Name="canvas" Background="White">
        </Canvas>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- X軸網格 -->
            <ItemsControl Grid.Row="0" Grid.Column="0">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <Canvas/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemContainerStyle>
                    <Style>
                        <Setter Property="Canvas.Left" Value="{Binding}"/>
                        <Setter Property="Shape.Stroke" Value="LightGray"/>
                        <Setter Property="Shape.StrokeThickness" Value="0.5"/>
                    </Style>
                </ItemsControl.ItemContainerStyle>
                <Rectangle Width="{Binding ActualWidth, ElementName=canvas}" Height="1"/>
            </ItemsControl>

            <!-- Y軸網格 -->
            <ItemsControl Grid.Row="0" Grid.Column="0">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <Canvas/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemContainerStyle>
                    <Style>
                        <Setter Property="Canvas.Top" Value="{Binding}"/>
                        <Setter Property="Shape.Stroke" Value="LightGray"/>
                        <Setter Property="Shape.StrokeThickness" Value="0.5"/>
                    </Style>
                </ItemsControl.ItemContainerStyle>
                <Rectangle Height="{Binding ActualHeight, ElementName=canvas}" Width="1"/>
            </ItemsControl>
        </Grid>
    </Grid>
</UserControl>
