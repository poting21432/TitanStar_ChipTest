<UserControl x:Class="Support.Wpf.UserControl_IniViewer"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Support.Wpf"
             mc:Ignorable="d" >
    <Grid>
        <ListView x:Name="ListView_IniView" ItemsSource="{Binding IniView}" Margin="10">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <GroupBox Header="{Binding Section}"  Margin="10">
                        <StackPanel>
                            <ListView ItemsSource="{Binding KeyDataList}">
                                <ListView.ItemTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal" Margin="10">
                                            <Label Content="{Binding Key}"/>
                                            <TextBox Text="{Binding Data}" Margin="10 0"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </ListView.ItemTemplate>
                            </ListView>
                        </StackPanel>
                    </GroupBox>
                </DataTemplate>
            </ListView.ItemTemplate>
            <ListView.ItemsPanel>
                <ItemsPanelTemplate>
                    <UniformGrid Columns="3"/>
                </ItemsPanelTemplate>
            </ListView.ItemsPanel>
        </ListView>  
    </Grid>
</UserControl>
